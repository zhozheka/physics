%движение Земли вокруг закрепленного Солнца
clear;
clc;
m = [1.9891*10^30; 
    5.9736*10^24; 
    7.3477*10^22; 
    2.2*10^14]; 
r = [0 0; 
    1.52098232*10^11 0; 
    1.52098232*10^11+3.84467*10^8 0; 
    5.24824*10^12 0]; 
v = [0 -0.089021687078894;
    0 29270; 
    0 29270+1023; 
    0 -900]; 

R = [ r(2,1), r(2,2), v(2,1), v(2,2)];

N = 1000
T = 3600*24*365*10;
dt= T / N;

for i=1:N
    
    %ERK1
    R = R + dt*F(R);
    
    %ERK2
    %k1 = R + dt*F(R);
    %R = R + (F(R)+F(k1))*dt/2;
    
    X(i) = R(1);   
    Y(i)=  R(2);
    
end
plot (X,Y)